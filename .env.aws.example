# =====================================================
# AWS Environment Variables for Docker Compose
# =====================================================
# Copy this file to .env.aws and fill in the values
# Usage: docker-compose --env-file .env.aws -f docker-compose.aws.yml up -d

# AWS Configuration
AWS_ACCOUNT_ID=937792903641
AWS_REGION=us-east-2

# RDS PostgreSQL Configuration (from Terraform output)
DB_HOST=restaurant-recommender-mlflow-db.xxxxx.us-east-2.rds.amazonaws.com
DB_NAME=mlflow
DB_USER=mlflow_admin
DB_PASSWORD=MLflow2025Secure!

# S3 Configuration
S3_BUCKET=foodie-ai-static-assets

# MLflow Configuration
MLFLOW_PORT=5000

# Application Ports
BACKEND_PORT=8000
FRONTEND_PORT=80
